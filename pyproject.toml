[project]
name = "dez-batch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "notebook>=7.5.3",
    "pandas>=3.0.0",
    "pyspark>=4.1.1",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
